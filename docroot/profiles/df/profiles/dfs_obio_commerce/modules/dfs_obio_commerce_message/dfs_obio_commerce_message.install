<?php

/**
 * @file
 * Contains installation and update routines for the DSF Obio Commerce Message module.
 */

/**
 * Implements hook_install().
 */
function dfs_obio_commerce_message_install() {
  // Rules cache is not normally cleared when new rules are added.
  \Drupal::cache('rules')->invalidateAll();
}
