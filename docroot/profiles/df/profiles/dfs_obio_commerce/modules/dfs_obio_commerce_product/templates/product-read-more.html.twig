{#
/**
 * @file
 * Default theme implementation for product read more link.
 *
 * To override output, copy the "product-read-more.html.twig" from the templates
 * directory to your theme's directory and customize it.
 *
 * Instead of overriding the theming for all fields, you can also just override
 * theming for a subset of fields using
 * @link themeable Theme hook suggestions. @endlink
 * Suggestions aren't provided by default. Implement
 * hook_theme_suggestions_HOOK_alter() to add ones.
 *
 * Available variables:
 * - attributes: HTML attributes for the containing element.
 * - title_attributes: HTML attributes for the title.
 * - content_attributes: dont use it, add the needed attributes to the url
 *   object directly or use the links_classes of this template.
 * - title: The title for the link.
 * - url: The url object of the link.
 *
 * @see template_preprocess_product_read_more()
 */
#}
{%
  set classes = [
    'product-read-more',
  ]
%}
{%
  set link_attributes = create_attribute(url.getOption('attributes') ? url.getOption('attributes') : []).setAttribute('href', url.toString())
%}
{%
  set link_classes = [
    'product-read-more__link',
  ]
%}
<div{{ attributes.addClass(classes) }}>
  <a{{ link_attributes.addClass(link_classes) }}>
    <span{{ title_attributes }}>{{ title }}</span>
  </a>
</div>
